describe('A high level test suite', () => {
	it('should get two numbers and return it\'s sum', () => {
		expect(1 + 2).toBe(3);
	});

	it('should get two numbers and return the subtracted value', () => {
		expect(4 - 1).toBe(3);
	});
});
